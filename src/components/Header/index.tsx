import { Box } from "@mui/material";
import { FC } from "react";

const Header: FC = () => {
  return (
    <header>
      <Box>
        <p>NFT Gallery</p>
      </Box>
    </header>
  );
};

export default Header;
